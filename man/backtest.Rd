% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backtest.R
\name{backtest}
\alias{backtest}
\title{Rolling-origin backtesting of lineage frequency models}
\usage{
backtest(
  data,
  engines = "mlr",
  origins = "weekly",
  horizons = c(7L, 14L, 21L, 28L),
  min_train = 42L,
  ...
)
}
\arguments{
\item{data}{An \link{lfq_data} object.}

\item{engines}{Character vector of engine names to compare.
Default \code{"mlr"}.}

\item{origins}{How to select forecast origins:
\itemize{
\item \code{"weekly"} (default): one origin per unique date, starting
after \code{min_train} days.
\item An integer: use every Nth date as an origin.
\item A Date vector: use these specific dates as origins.
}}

\item{horizons}{Integer vector of forecast horizons in days.
Default \code{c(7, 14, 21, 28)}.}

\item{min_train}{Minimum training window in days. Origins earlier
than \code{min(date) + min_train} are skipped. Default 42 (6 weeks).}

\item{...}{Additional arguments passed to \code{\link[=fit_model]{fit_model()}} (e.g.,
\code{generation_time} for the Piantham engine).}
}
\value{
An \code{lfq_backtest} object (tibble subclass) with columns:
\describe{
\item{origin_date}{Date used as the training cutoff.}
\item{target_date}{Date being predicted.}
\item{horizon}{Forecast horizon in days.}
\item{engine}{Engine name.}
\item{lineage}{Lineage name.}
\item{predicted}{Predicted frequency (median).}
\item{lower}{Lower prediction bound.}
\item{upper}{Upper prediction bound.}
\item{observed}{Observed frequency at target_date.}
}
}
\description{
Evaluates forecast accuracy by repeatedly fitting models on
historical data and comparing predictions to held-out observations.
This implements the evaluation framework described in Abousamra
et al. (2024).
}
\examples{
\donttest{
sim <- simulate_dynamics(n_lineages = 3,
  advantages = c("A" = 1.2, "B" = 0.8),
  n_timepoints = 20, seed = 1)
bt <- backtest(sim, engines = "mlr",
  horizons = c(7, 14), min_train = 42)
bt
}

}
\seealso{
\code{\link[=score_forecasts]{score_forecasts()}} to compute accuracy metrics,
\code{\link[=compare_models]{compare_models()}} to rank engines.
}
