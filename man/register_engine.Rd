% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine-registry.R
\name{register_engine}
\alias{register_engine}
\title{Register a custom modeling engine}
\usage{
register_engine(
  name,
  fit_fn,
  description = "",
  type = "frequentist",
  time_varying = FALSE
)
}
\arguments{
\item{name}{Engine name (character scalar).}

\item{fit_fn}{Function with signature \verb{function(data, pivot, ci_level, ...)}.
Must return a list compatible with \code{lfq_fit} structure.}

\item{description}{One-line description of the engine.}

\item{type}{\code{"frequentist"} or \code{"bayesian"}.}

\item{time_varying}{Logical; does the engine support time-varying
growth advantages?}
}
\value{
Invisibly returns the updated engine registry.
}
\description{
Allows third-party packages to register custom engines with
\code{fit_model()}. This enables an extensible plugin architecture
similar to 'parsnip' engine registration.
}
\examples{
# Register a custom engine
my_engine <- function(data, pivot = NULL, ci_level = 0.95, ...) {
  # Custom implementation...
  .engine_mlr(data, pivot = pivot, ci_level = ci_level, ...)
}
register_engine("my_mlr", my_engine, "Custom MLR wrapper")
lfq_engines()

}
