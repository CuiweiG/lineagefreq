% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lfq-data.R
\name{lfq_data}
\alias{lfq_data}
\title{Create a lineage frequency data object}
\usage{
lfq_data(
  data,
  lineage,
  date,
  count,
  total = NULL,
  location = NULL,
  min_total = 10L
)
}
\arguments{
\item{data}{A data frame containing at minimum columns for lineage
identity, date, and count.}

\item{lineage}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Column
containing lineage/variant identifiers (character or factor).}

\item{date}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Column
containing collection dates (\code{Date} class or parseable character).}

\item{count}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Column
containing sequence counts (non-negative integers).}

\item{total}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Optional
column of total sequences per date-location. If \code{NULL}, computed
as the sum of \code{count} per group.}

\item{location}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Optional
column for geographic stratification.}

\item{min_total}{Minimum total count per time point. Time points
below this are flagged as unreliable. Default 10.}
}
\value{
An \code{lfq_data} object (a tibble subclass) with standardized
columns:
\describe{
\item{\code{.lineage}}{Lineage identifier (character).}
\item{\code{.date}}{Collection date (Date).}
\item{\code{.count}}{Sequence count (integer).}
\item{\code{.total}}{Total sequences at this time point (integer).}
\item{\code{.freq}}{Observed frequency (numeric).}
\item{\code{.reliable}}{Logical; \code{TRUE} if \code{.total >= min_total}.}
\item{\code{.location}}{Location, if provided (character).}
}
All original columns are preserved.
}
\description{
Validates, structures, and annotates lineage count data for
downstream modeling and analysis. This is the entry point for
all lineagefreq workflows.
}
\details{
Performs the following validation and processing:
\enumerate{
\item Checks that all required columns exist and have correct types.
\item Coerces character dates to Date via ISO 8601 parsing.
\item Ensures counts are non-negative integers.
\item Replaces NA counts with 0 (with warning).
\item Aggregates duplicate lineage-date rows by summing (with warning).
\item Computes per-time-point totals and frequencies.
\item Flags time points below \code{min_total} as unreliable.
\item Sorts by date ascending, then lineage alphabetically.
}
}
\examples{
d <- data.frame(
  date = rep(seq(as.Date("2024-01-01"), by = "week",
                 length.out = 8), each = 3),
  lineage = rep(c("JN.1", "KP.3", "Other"), 8),
  n = c(5, 2, 93, 12, 5, 83, 28, 11, 61, 50, 20, 30,
        68, 18, 14, 80, 12, 8, 88, 8, 4, 92, 5, 3)
)
x <- lfq_data(d, lineage = lineage, date = date, count = n)
x

}
