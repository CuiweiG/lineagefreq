% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read-counts.R
\name{read_lineage_counts}
\alias{read_lineage_counts}
\title{Read lineage count data from a CSV file}
\usage{
read_lineage_counts(
  file,
  date = "date",
  lineage = "lineage",
  count = "count",
  ...
)
}
\arguments{
\item{file}{Path to CSV file.}

\item{date}{Name of the date column. Default \code{"date"}.}

\item{lineage}{Name of the lineage column. Default \code{"lineage"}.}

\item{count}{Name of the count column. Default \code{"count"}.}

\item{...}{Additional arguments passed to \code{\link[=lfq_data]{lfq_data()}}.}
}
\value{
An \link{lfq_data} object.
}
\description{
A convenience wrapper for reading surveillance count data from
CSV files into \link{lfq_data} format. Expects a file with at least
columns for date, lineage, and count.
}
\examples{
# Read the bundled example CSV
f <- system.file("extdata", "example_counts.csv",
                 package = "lineagefreq")
x <- read_lineage_counts(f)
x

}
