% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{collapse_lineages}
\alias{collapse_lineages}
\title{Collapse rare lineages into an aggregate group}
\usage{
collapse_lineages(
  x,
  min_freq = 0.01,
  min_count = 10L,
  other_label = "Other",
  mapping = NULL
)
}
\arguments{
\item{x}{An \link{lfq_data} object.}

\item{min_freq}{Minimum peak frequency a lineage must reach at
any time point to be kept. Default 0.01 (1\%).}

\item{min_count}{Minimum total count across all time points to
be kept. Default 10.}

\item{other_label}{Label for the collapsed group. Default "Other".}

\item{mapping}{Optional named character vector for custom grouping.
Names = original lineage names, values = group names. If provided,
\code{min_freq} and \code{min_count} are ignored.}
}
\value{
An \link{lfq_data} object with rare lineages merged.
}
\description{
Merges lineages that never exceed a frequency or count threshold
into a single group (default "Other"). Useful for reducing noise
from dozens of low-frequency lineages.
}
\examples{
sim <- simulate_dynamics(n_lineages = 6,
  advantages = c(A = 1.3, B = 1.1, C = 0.95, D = 0.5, E = 0.3),
  n_timepoints = 12, seed = 1)
collapsed <- collapse_lineages(sim, min_freq = 0.05)
attr(collapsed, "lineages")

}
