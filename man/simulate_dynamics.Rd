% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_dynamics}
\alias{simulate_dynamics}
\title{Simulate lineage frequency dynamics}
\usage{
simulate_dynamics(
  n_lineages = 4L,
  n_timepoints = 20L,
  total_per_tp = 500L,
  advantages = NULL,
  reference_name = "ref",
  start_date = Sys.Date(),
  interval = 7L,
  overdispersion = NULL,
  seed = NULL
)
}
\arguments{
\item{n_lineages}{Number of lineages including reference. Default 4.}

\item{n_timepoints}{Number of time points. Default 20.}

\item{total_per_tp}{Sequences per time point. A single integer
(constant across time) or a vector of length \code{n_timepoints}
(variable sampling effort). Default 500.}

\item{advantages}{Named numeric vector of per-week multiplicative
growth advantages for non-reference lineages. Length must equal
\code{n_lineages - 1}. Values > 1 mean growing faster than reference,
< 1 means declining. If \code{NULL}, random values in (0.8, 1.5).}

\item{reference_name}{Name of the reference lineage. Default \code{"ref"}.}

\item{start_date}{Start date for the time series. Default today.}

\item{interval}{Days between consecutive time points. Default 7
(weekly data).}

\item{overdispersion}{If \code{NULL} (default), standard multinomial
sampling. If a positive number, Dirichlet-Multinomial sampling
with concentration = 1 / overdispersion. Larger values = more
overdispersion.}

\item{seed}{Random seed for reproducibility.}
}
\value{
An \link{lfq_data} object with an additional \code{true_freq} column
containing the true (pre-sampling) frequencies.
}
\description{
Generates synthetic lineage frequency data under a multinomial
sampling model with configurable growth advantages. Useful for
model validation, power analysis, and teaching.
}
\examples{
# JN.1 grows 1.3x per week, KP.3 declines at 0.9x
sim <- simulate_dynamics(
  n_lineages = 3,
  advantages = c("JN.1" = 1.3, "KP.3" = 0.9),
  n_timepoints = 15,
  seed = 42
)
sim

}
